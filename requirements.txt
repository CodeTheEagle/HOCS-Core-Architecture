# HOCS Core Dependencies
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0

# API & Server
fastapi>=0.68.0
uvicorn>=0.15.0
pydantic>=1.8.0
python-multipart>=0.0.5

# Hardware Interface (On Device Only)
# Note: PYNQ is pre-installed on Kria images, but listed here for reference.
pynq>=2.7.0; sys_platform == 'linux'

# Simulation & Visualization
matplotlib>=3.4.0
pyyaml>=5.4.0

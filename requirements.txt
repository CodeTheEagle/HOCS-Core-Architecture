# HOCS (Hybrid Optical Computing System) Dependency Manifest
# ==========================================================
# Maintainer: Muhammed Yusuf Cobanoglu
# Target Platform: Linux (Ubuntu/Kria OS) & Python 3.8+

# --- 1. CORE MATHEMATICS & PHYSICS SIMULATION ---
# Temel matris islemleri ve optik sinyal simulasyonu icin olmazsa olmazlar.
numpy>=1.24.0      # Matrix manipulation (The backbone of everything)
scipy>=1.10.0      # Advanced signal processing for simulating analog noise

# --- 2. DEEP LEARNING & COMPILER STACK ---
# PyTorch modellerini alip kendi optik Assembly dilimize cevirmek icin.
torch>=2.0.0       # Neural Network graph tracing (fx module)
torchvision>=0.15.0 # Pre-trained vision models for benchmarking

# --- 3. HARDWARE INTERFACE (FPGA/EDGE) ---
# Xilinx Kria uzerindeki donanima (DMA, Register) erismek icin.
# Not: PC'de simulasyon modunda bu kutuphaneler mocklanir.
pynq>=3.0.1        # Xilinx Python Productivity for Zynq (Hardware Overlay)
smbus2>=0.4.2      # I2C protocol for reading voltage sensors

# --- 4. BACKEND API & DRIVER SERVICE ---
# Optik cekirdegi dis dunyaya (veya Docker icine) servis etmek icin.
fastapi>=0.95.0    # High-performance Async API framework
uvicorn>=0.22.0    # ASGI Server to run the API
pydantic>=1.10.0   # Data validation for incoming matrix payloads
requests>=2.28.0   # For client-side testing scripts

# --- 5. DEVELOPMENT & TESTING ---
# Kod kalitesini korumak ve CI/CD surecleri icin.
pytest>=7.3.0      # Unit testing framework
pytest-asyncio     # Testing async API endpoints
black>=23.3.0      # Code formatter (Because style matters)

# --- 6. UTILITIES ---
tqdm>=4.65.0       # Progress bars for long-running benchmarks
psutil>=5.9.0      # Monitoring CPU temps and RAM usage during stress tests
